{
  "name": "endolib",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint || true"
  },
  "devDependencies": {
    "@types/node": "^24.5.2"
  }
}
